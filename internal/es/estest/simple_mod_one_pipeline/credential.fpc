credential "aws" "example" {
    access_key = "bbbb"
    secret_key = "abcdefgsfd"
}

credential "aws" "example_two" {
    access_key = "AAAA"
    secret_key = "BBBB"
}

credential "aws" "sso" {
    access_key = "sso_key"
    secret_key = "sso_secret"
}

credential "aws" "dundermifflin" {
    access_key = "dundermifflin_key"
    secret_key = "dundermifflin_secret"
}
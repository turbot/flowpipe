notifier "admin" {

    title = "admin"

    notify {
        integration = integration.http.default
    }

    notify {
        integration = integration.email.my_email_app
        to = ["foo", "bar"]
    }
}